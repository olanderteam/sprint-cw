# Proxy Server Configuration

# Server port
PORT=3001

# Jira credentials
JIRA_DOMAIN=your-domain.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your-api-token

# Cache configuration (in seconds)
CACHE_TTL=120

# Environment
NODE_ENV=development

# Optional: Filter boards by project keys (comma-separated)
# Example: JIRA_PROJECT_KEYS=AC,AO,BLOG,CDM,CHAP,CHN,CONT,CRI,CRON,DEV,EM,FDI,FDP,GH,GWT,IM,LDC,SCC
# Leave empty or comment out to fetch all boards
JIRA_PROJECT_KEYS=
